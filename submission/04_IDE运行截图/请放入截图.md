# 待补充：IDE 运行截图

请至少放入以下截图：

1. NucleiStudio 的 Project Explorer，能看到完整 `qemu` 工程。
2. Build Console 显示 `Build Finished. 0 errors, 0 warnings`。
3. Debug Console 显示 `soc init done`、`Board: Qemu` 和手势输出。
4. 运行到 320000 Byte 后显示 `imu crc8/smbus 320000 bytes: 0x...`。

建议使用 PNG 原图，保证文字清晰，不要裁掉 IDE 标题栏和工程名称。
